<%= form_with(model: recipient_list, local: true) do |form| %>
  <% if recipient_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipient_list.errors.count, "error") %> prohibited this recipient_list from being saved:</h2>

      <ul>
        <% recipient_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group row">
    <%= form.label :name, class: 'col-sm-2 col-form-label' %>
    <%= form.text_field :name, class: 'col-sm-10' %>
  </div>

  <div class="field form-group row">
    <%= form.label :notes, class: 'col-sm-2 col-form-label' %>
    <%= form.text_field :notes, class: 'col-sm-10' %>
  </div>

  <div class="field form-group row">
    <%= form.label :keyword, class: 'col-sm-2 col-form-label' %>
    <%= form.text_field :keyword, class: 'col-sm-10' %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
